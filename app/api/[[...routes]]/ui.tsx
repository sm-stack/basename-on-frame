/** @jsxImportSource frog/jsx */
import { createSystem, colors } from "frog/ui";

export const {
  Box,
  Columns,
  Column,
  Heading,
  HStack,
  Icon,
  Rows,
  Row,
  Spacer,
  Text,
  VStack,
  vars,
} = createSystem({
  colors: {
    black: "#000000",
    white: "#ffffff",
  },
})
